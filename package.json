{
  "name": "delvepack",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Co-op top-down dungeon crawler — descend a monster-choked dungeon, clear each floor, grab upgrades, revive each other. Solo or peer-to-peer with friends.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ben-gy/game-engine": "github:ben-gy/gh-game-engine#v1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
